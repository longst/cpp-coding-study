# Error Log

Use this file to track recurring C++ mistakes, why they happen, and the correct pattern.

| Date | Topic | Mistake | Why it fails | Correct pattern |
|---|---|---|---|---|
| 2026-09-05 | Palindrome number | Returned `false` for one-digit numbers | One-digit numbers are palindromes | Let the two-pointer loop handle length 1, or return `true` |
| 2026-09-05 | Palindrome number | Wrote `if (int < 0)` | `int` is a type, not the variable name | Use `if (input < 0)` |
| 2026-09-05 | Valid parentheses | Tried matching only adjacent characters | Valid brackets may be nested, not adjacent | Use `std::stack<char>` |
| 2026-09-06 | Merge two sorted lists | Used `list1->next` in loop condition | Crashes if `list1 == nullptr`; also skips last node | Use `while (list1 != nullptr && list2 != nullptr)` |
| 2026-09-06 | Merge two sorted lists | Wrote `currentNode = list1` | Reassigns local pointer but does not connect the list | Use `currentNode->next = list1` |
| 2026-09-06 | Merge two sorted lists | Returned dummy node itself | Dummy is only an auxiliary node | Return `dummy.next` |
| 2026-09-06 | Remove duplicates | Used `unordered_set` | It does not preserve sorted order and is not in-place | Use two pointers/write index |
| 2026-09-06 | Remove element | Used `nums[i] = nums[i + 1]` | Last iteration reads out of bounds and does not solve repeated removals | Use a write index for values not equal to `val` |
| 2026-09-06 | Remove element | Returned removed count | Problem asks for kept count `k` | Return `writeIndex` |
| 2026-09-06 | String search | Called `sub_string.substr(...)` while declaring `sub_string` | Source string should be `haystack` | Use `haystack.substr(i, needle.size())` |
| 2026-09-06 | String search | Used `needle.size() + 1` | Substring length should equal needle length | Use `needle.size()` |
| 2026-09-06 | String search | Loop used `<` instead of `<=` | Misses the last valid starting index | Use `i <= haystack.size() - needle.size()` |
| 2026-09-06 | Last word length | Used `words[words.size()]` | Vector indices end at `size() - 1` | Use `words.back()` or `words[words.size() - 1]` |
| 2026-09-06 | Search insert position | Used `middle = min + max / 2` | Incorrect midpoint when left boundary is not zero | Use `left + (right - left) / 2` |
| 2026-09-06 | Search insert position | Accessed `middle + 1` and `middle - 1` | Can go out of bounds | Use standard binary search boundaries |
| 2026-09-06 | Plus one | Converted digit vector to integer | Large input can overflow integer types | Process digits from right to left with carry |
| 2026-09-07 | Plus one | Started loop at `digits.size()` | Valid last index is `digits.size() - 1` | Start at `static_cast<int>(digits.size()) - 1` |
| 2026-09-18 | Longest substring | Used the window set's size as the input loop bound and erased the right character on duplicates | The set changes size and the window must shrink from its left edge | Bound `right` by `s.size()` and erase `s[left++]` until the duplicate is gone |
| 2026-09-18 | Zigzag conversion | Allocated row strings but indexed characters inside still-empty strings | Allocating the outer vector does not allocate indexed characters in each string | Append with `rows[currentRow].push_back(ch)` and toggle direction at row bounds |
| 2026-09-18 | Majority element | Used `=` instead of `==` in Boyer-Moore conditions and wrote `dic[i] = dic[i]++` | Assignment mutates state, and post-increment returns the old value that was assigned back | Compare with `==`; count with `++dic[value]` |
| 2026-09-18 | Excel column number | Converted each character correctly but did not accumulate its position | Each new digit must shift the previous prefix by one base-26 place | Use `total = total * 26 + (ch - 'A' + 1)` |
| 2026-09-18 | Reverse bits | Inserted a bit before shifting an uninitialized result | The final inserted bit was shifted once too far, and uninitialized reads are invalid | Start with `uint32_t result = 0`, then use `result = (result << 1) \| (n & 1)` |
| 2026-09-18 | Number of 1 bits | Tested `n & 1` while shifting a different variable | The same original bit was inspected repeatedly | Test and shift the same variable, or clear bits with `n &= n - 1` |
| 2026-09-18 | Happy number | Used undeclared `current` and continued only when `n` was already in the set | The code does not compile, and a new starting number makes the loop stop immediately | Use `n` consistently and continue while `n != 1 && !seen.contains(n)` |
| 2026-09-18 | Remove linked-list elements | Started traversal at the dummy node without connecting `dummy.next` to `head` | The dummy node is not part of the input list, so the original nodes are never visited | Set `dummy.next = head` and start `current` at `&dummy` |
| 2026-09-18 | Remove linked-list elements | Returned `dummy->next` when `dummy` is an object | `->` is for pointers, but `dummy` is a stack object | Return `dummy.next` |
| 2026-09-18 | Remove linked-list elements | Advanced `current` unconditionally after removing `current->next` | Consecutive matches are skipped, and removing the last node can make `current` null before the next condition check | Advance `current` only in the `else` branch when no node is removed |
| 2026-09-18 | Isomorphic strings | Compared an `int` map value with `nullptr` and used `operator[]` to test whether a key exists | `nullptr` is for pointers, and `operator[]` inserts a missing key with value `0` | Test existence with `find()` or `contains()` |
| 2026-09-18 | Isomorphic strings | Checked only the mapping from `s` to `t` | A one-way check allows two different source characters to map to the same target character | Store actual character mappings in both directions |
| 2026-09-18 | Isomorphic strings | Wrote `tTos` instead of the declared variable `tToS` | C++ names are case-sensitive, so the undeclared identifier causes a compile error | Use the exact declared capitalization consistently |
| 2026-09-18 | Reverse linked list | Pointed the next node back to `head` without first saving the remaining list and changing `head->next` | The two nodes point to each other, creating a cycle and preventing forward traversal | Save `next`, point `current->next` to `previous`, then advance both pointers |
| 2026-09-18 | Reverse linked list | Returned an uninitialized temporary pointer for a one-node list | The loop never runs, so the pointer has an indeterminate value | Initialize `previous` to `nullptr` and return it after processing every node |
| 2026-09-18 | Reverse linked list | Saved `nextNode` but never updated `previous`, then returned `current` | Every node is pointed to `nullptr`, and `current` is always `nullptr` when the loop ends | After reversing the link, set `previous = current`, advance `current`, and return `previous` |
| 2026-09-18 | Contains duplicate | Wrote `std::unorder_set` instead of `std::unordered_set` | The misspelled container type does not exist, causing a compile error | Use `std::unordered_set<int>` |
| 2026-09-19 | Contains Duplicate II | Used `<= nums.size()` as an index bound and wrote `num[i]` instead of `nums[i]` | Index `size()` is out of bounds, and the misspelled variable causes a compile error | Use `< nums.size()` and the exact declared name `nums` |
| 2026-09-19 | Contains Duplicate II | Compared every pair before checking whether their indices were close | This works after syntax fixes but takes `O(n^2)` time and may time out | Limit the inner range to `i + k`, or use a sliding-window `unordered_set` |
| 2026-09-19 | Contains Duplicate II | Used `nums.size() - 1` as a loop limit | `size()` is unsigned, so subtracting one from an empty container underflows before the cast | Prefer `i < static_cast<int>(nums.size())` |
| 2026-09-19 | C++ syntax | Typed `< =` and the full-width punctuation `；` | Operators cannot contain whitespace, and Chinese punctuation is not a C++ statement terminator | Write `<=` with no space and use the ASCII semicolon `;` |
| 2026-09-19 | Contains Duplicate II | Wrote `a_set.erase[i - k - 1]` while implementing the sliding window | Member functions require parentheses, and the set stores array values rather than their indices | Remove the expired value with `a_set.erase(nums[i - k - 1])` |
| 2026-09-19 | Stack using queues | Called `front_queue.empty()` without returning its result | The function promises a `bool` but reaches the end without returning a value | Write `return front_queue.empty();` |
| 2026-09-19 | Stack using queues | Tried to use normal queue order directly for stack removal | A queue exposes the oldest item, while a stack must remove the newest item | Move all but the final queue element into a helper queue, remove the final element, then swap the queues |
| 2026-09-19 | Summary ranges | Built a range by concatenating numbers and taking the first and last characters | Characters are not complete numbers, so multi-digit and negative values produce incorrect endpoints | Track integer `start` and `end` values, then convert each endpoint with `std::to_string()` |
| 2026-09-19 | Summary ranges | Used `nums.size() - 1` as the loop bound and emitted a range only when a gap was found | Empty input can cause unsigned underflow, and the final range has no following gap so it is omitted | Use indices bounded by `< nums.size()` and explicitly emit each completed range |
| 2026-09-19 | Summary ranges | Used `< nums.size() - 1` for both range-scanning conditions | The last element is excluded, a one-element input is never processed, and empty input can underflow | Use `i < nums.size()` outside and `i + 1 < nums.size()` inside |
| 2026-09-19 | Summary ranges | Did not increment `i` after adding a completed range | If the inner loop stops at a gap, the outer loop processes the same index forever | Add `++i` after appending each range |
| 2026-09-19 | Power of two | Counted set bits by right-shifting a signed integer without rejecting negative input | Right-shifting a negative signed value is implementation-defined and commonly keeps the sign bit, so the loop may never reach zero | Return `false` for `n <= 0`, then count bits using an unsigned value or use `n & (n - 1)` |
| 2026-09-19 | Queue using stacks | Used the return value of `stack::pop()` | `pop()` returns `void`; it removes the top element but does not return it | Read with `top()` first, then call `pop()` |
| 2026-09-19 | Queue using stacks | Used a `for` bound based on `front_stack.size()` while popping from that stack | The size shrinks during the loop, so only part of the stack may be transferred | Use `while (front_stack.size() > 1)` or the standard input/output stack pattern |
| 2026-09-20 | Queue using stacks | Assumed swapping the main and helper stacks would preserve the original representation | Transferring between stacks reverses order, so after the swap the oldest remaining element changes from the bottom to the top and later operations follow the wrong invariant | Move elements back, or keep separate input/output stacks with distinct roles |
| 2026-09-20 | Palindrome linked list | Returned `false` when `head != nullptr` | Every non-empty input exits immediately, while the problem guarantees at least one node | Treat zero or one node as a palindrome, then process longer lists |
| 2026-09-20 | Palindrome linked list | Wrote `=!` instead of `!=` in loop and value comparisons | `=!` assigns the negated right-hand value; it is not the not-equal operator | Use `!=` for comparisons and avoid assignment inside conditions |
| 2026-09-20 | Palindrome linked list | Compared node pointers and did not advance either comparison pointer | Palindrome checking compares values at different nodes, and unchanged pointers cause an infinite loop | Compare `left->val` with `right->val`, then advance both pointers |
| 2026-09-20 | Reverse linked list helper | Used `while (current->next != nullptr)` and returned `current` without reversing its link | The final node is never connected to the reversed prefix, so most of the reversed list is lost | Loop while `current != nullptr` and return `previous` |
| 2026-09-20 | Palindrome linked list | Returned immediately after finding unequal values | The reversed second half is not restored on the failure path | Save the comparison result, restore the list, then return the result |
| 2026-09-20 | Palindrome linked list | Tried to restore the list with the original middle pointer after reversing it | After reversal, that pointer is the tail of the reversed half rather than its head | Save the head returned by reversal and pass that pointer to the restoring reversal |
| 2026-09-20 | Valid anagram | Compared only the sum of character-code differences | Different character combinations can have the same total code sum, causing false positives | Count the frequency of every character and verify that all counts balance |
| 2026-09-20 | Binary tree paths | Used one vector for all left visits and one for all right visits | A binary tree may contain many branching root-to-leaf paths, not just one left path and one right path | Carry one current path through DFS and append a completed path at each leaf |
| 2026-09-20 | Binary tree paths | Called the recursive helper without its required path arguments and returned no result from `binaryTreePaths` | The code does not compile, and the public function must return `vector<string>` | Pass all helper arguments and return the collected string paths |
| 2026-09-20 | Binary tree paths | Passed the current path by non-const reference and modified it without backtracking | Changes made while traversing the left subtree remain when traversing the right subtree, mixing separate paths | Pass the path by value, or restore its previous length after each recursive branch |
| 2026-09-20 | Binary tree paths | Passed the temporary string `""` to a `std::string&` parameter | A non-const lvalue reference cannot bind to a temporary value | Pass the path by value or create a named `std::string` variable |
| 2026-09-20 | Add digits | Wrote `num / 10` without assigning the result | Division computes a value but does not modify `num`, so a positive input makes the loop run forever | Use `num /= 10` or `num = num / 10` |
| 2026-09-20 | Add digits | Repeated digit summing only while `num > 10` | The two-digit value `10` is skipped and incorrectly returned unchanged | Continue while `num >= 10` |
| 2026-09-20 | Ugly number | Started repeated factor division without rejecting `n == 0` | `0 % factor` is always zero and `0 / factor` remains zero, so the loop never ends | Return `false` immediately when `n <= 0` |
